	this is the first file for learning git.
	the directory 'e:/gitlearing' is under the git's management.
	不要随便更改文件夹‘gitlearing’下的文件，这个文件夹是git仓库初始化的文件夹，用于学习git.
	在本地gitLearning文件夹下有.git隐藏文件夹，这是git仓库的文件.
	在github上对应的远端仓库是learningGit.
